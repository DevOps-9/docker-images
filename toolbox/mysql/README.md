MySQL toolbox
=============

A set of tools to query/inspect MySQL.

Run it:

```bash
$ docker run --rm -it  deviantony/toolbox:mysql zsh
```

Available tools
---------------

* MySQL client
* innotop
