MongoDB toolbox
===============

A set of tools to query/inspect MongoDB.

Run it:

```bash
$ docker run --rm -it  deviantony/toolbox:mongodb zsh
```

Available tools
---------------

* Mongo shell (> 3.0)
* Mongotop
* Mongostat
